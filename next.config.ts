import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["www.qatarairways.com", "www.goindigo.in"], // ✅ Replace with the actual domain(s) hosting your images
  },
};

export default nextConfig;
